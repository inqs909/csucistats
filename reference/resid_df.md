# Extract Residuals and Influential Measures from \`glm\` and \`lm\` object

Extract Residuals and Influential Measures from \`glm\` and \`lm\`
object

## Usage

``` r
resid_df(object)
```

## Arguments

- object:

  An R object that contains the results of the \`lm\` or \`glm\`
  function.
